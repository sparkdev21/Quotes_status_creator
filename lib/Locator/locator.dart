import 'package:get_it/get_it.dart';

final locator = GetIt.instance;

void setup() {
  // locator.registerSingleton<ScreenshotController>(ScreenshotController());

// Alternatively you could write it if you don't like global variables
}
